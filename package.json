{
  "name": "xigai-cloudflare-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy:pages": "wrangler pages deploy public",
    "check": "node --check functions/api/ai.js && node --check public/assets/js/main.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}